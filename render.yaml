services:
  - type: web
    name: zap-insolite-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    plan: free
    build:
      packages:
        - ffmpeg
