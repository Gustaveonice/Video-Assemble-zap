services:
  - type: web
    name: zap-insolite-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    plan: free
    build:
      packages:
        - ffmpeg
